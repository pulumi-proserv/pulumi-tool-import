bin
dist
.terraform
**/terraform.tfstate.backup
pulumi-tool-import
**/.terraform.lock.hcl
# ...but keep committed lockfile fixtures that tests depend on
!**/testdata/**/.terraform.lock.hcl

# Local direnv config (copy from .envrc.example)
.envrc

go.work
go.work.sum
test-results/
.clj-kondo/
.lsp/

# Superpowers working documents (plans, specs) are session artefacts, not
# repo content. The SDD ledger under .superpowers/ is already ignored; these
# were committed by oversight.
docs/superpowers/
